/**
 * Placeholder seed runner (scaffold).
 *
 * The real seeder is implemented alongside the Governance Kernel pass.
 * For now this only reports intent and exits cleanly so `npm run db:seed` is wired.
 */

function main(): void {
  console.log('[db:seed] Placeholder — no seed data is applied in the scaffold.');
  console.log('[db:seed] See db/seed/README.md for conventions.');
}

main();
